import SwiftUI
import SFSymbolsKit

struct ContentView: View {
    var body: some View {
        // Typed: autocompleted, refactor-safe, compile-checked.
        Image(systemName: String.SFSymbols.squareAndArrowUp)
    }
}
